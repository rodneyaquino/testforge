{
  "name": "testforge-web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "playwright test -c ./playwright.config.ts",
    "test:headed": "playwright test -c ./playwright.config.ts --headed",
    "test:debug": "PWDEBUG=1 playwright test -c ./playwright.config.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@axe-core/playwright": "^4.8.0",
    "typescript": "^5.6.3"
  }
}